# ============================================================================
# CONFIGURACIÓN DE API DE OPENAI
# ============================================================================
# Reemplaza los valores con los tuyos reales

# Tu clave API de OpenAI (obligatorio para usar la API)
# Obtén la tuya en: https://platform.openai.com/api/keys
LLM_API_KEY=sk-proj-tu-clave-api-aqui

# Modelo de OpenAI a utilizar
# Opciones: gpt-4, gpt-4-turbo, gpt-4o-mini, gpt-3.5-turbo
LLM_MODEL=gpt-4o-mini

# URL base de la API de OpenAI (normalmente no es necesario cambiar)
LLM_API_URL=https://api.openai.com/v1

# Timeout en segundos para las llamadas a la API
LLM_TIMEOUT=30

# ============================================================================
# CONFIGURACIÓN AVANZADA (Opcional)
# ============================================================================

# Si quieres forzar el uso de recomendaciones simuladas (sin IA real)
# Descomenta la línea siguiente y cambia a "true" si es necesario
# USE_MOCK_RECOMMENDATIONS=false

# Carpeta donde se guardarán los archivos cargados
UPLOAD_FOLDER=uploads

# Clave secreta de Flask (cambiar en producción)
SECRET_KEY=dev-secret-key-change-in-production
